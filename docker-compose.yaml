services:
  proxy:
    build: .
    container_name: teldrive-python-proxy
    env_file:
      - .env
    # Increase file descriptor limits for high-performance streaming
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    restart: unless-stopped
    network_mode: host